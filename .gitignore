obj/
bin/
tmp/
.vscode/
sign.cmd
.aider*
__pycache__/
# Syncfusion license key - never commit this
EQLogParser/SyncfusionKey.cs
EQLogParser/data/spells_old.txt
EQLogParserInstall/Output/
CLAUDE.md
BACKLOG.md
# Claude-purposed process runbooks (see memory `feedback_claude_files_gitignored`).
# Read as general maintainer docs; revisit tracking if the project becomes collaborative.
docs/ReleaseProcedure.md
docs/UpstreamMerge.md
.opencode
.claude/
# Local reference clone of ngdeao/SoD-winspellparser (third-party tool). Our
# spell converter / validator cite its SpellParser.cs column layout; kept local
# for offline lookup, not vendored into the fork. See tools/spells/README.md.
tools/SoD-winspellparser/
# Captured fixtures and snapshot baselines for the integration regression tests
# (see EQLogParser.Test/src/integration). Each developer's logs + JSON baselines
# stay local — see memory `project_baseline_regression_test` for rationale.
EQLogParser.Test/Resources/baseline-logs/
